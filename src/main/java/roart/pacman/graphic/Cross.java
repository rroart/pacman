package roart.pacman.graphic;

import roart.pacman.game.Colour;

public class Cross extends Walls {
         
    public Cross() {
 
	consfn();
	pix(Shapes.CROSS_BITS,Colour.getWallcolour(),Colour.getMybackground());
	
    };

}
